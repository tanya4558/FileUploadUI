<Window x:Class="FileUploadTool.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileUploadTool.Views"
        mc:Ignorable="d"
    Title="File Upload Client" Height="450" Width="800">
    <Grid Margin="10">
        <StackPanel>
            <Button Content="Select Files" Command="{Binding SelectFilesCommand}" Margin="0,0,0,10"/>
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Content="Start Upload" Command="{Binding StartUploadCommand}" Margin="0,0,10,0" Grid.Column="0"/>
                <Button Content="Pause" Command="{Binding PauseCommand}" Margin="0,0,0,0" Grid.Column="1"/>
            </Grid>
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Content="Resume" Command="{Binding ResumeCommand}" Margin="0,0,10,0" Grid.Column="0"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="0,0,10,0" Grid.Column="1"/>
                <Button Content="Clear" Command="{Binding ClearCommand}" Margin="0,0,0,0" Grid.Column="2"/>
            </Grid>
            <ListView ItemsSource="{Binding Files}" Height="250">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="File Name" DisplayMemberBinding="{Binding FileName}" Width="400"/>
                        <GridViewColumn Header="Status" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Status}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</Window>
